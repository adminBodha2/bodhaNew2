Each page has a wrapper, so it imports a container from the comps folder, wrapper.svelte, and that wraps everything inside the page. Then inside the pages, we have sections. A section is called div class box, and each section has the classes padded and wraps. So each outer section is a box with padded and wraps. These are the three classes that each outer section must have, and inside that, there should be areas.